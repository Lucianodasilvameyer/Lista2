using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class treinoNoInimigo2 : MonoBehaviour
{
    [SerializeField]
    private float speed;

    [SerializeField]
    private float strenght;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
